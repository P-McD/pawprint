import { describe } from "vitest";
import AddToBag from "~/utils/AddToBag";

describe("AddToBag", () => { 
    test("should successfully add a cart item to the bag", async () => {
     //TODO: rewrite this test before continuing
    })
 })